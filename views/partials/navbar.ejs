<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
                aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
                <div class="navbar-nav gap-2">
                    <a class="nav-link btn btn-secondary " href="/">Home</a>
                    <a class="nav-link btn btn-secondary" href="/campgrounds">Campgrounds</a>
                   
                </div>
                 <div class="navbar-nav ms-auto gap-2">
                     <% if(!currentUser) {%> 
                    <a class="nav-link btn btn-secondary " href="/login">Login</a>
                    <a class="nav-link btn btn-secondary" href="/register">Signup</a>
                    <% } else { %> 
                    <a href="" class="nav-link"><%= currentUser.username %></a>
                    <a class="nav-link" href="/logout">Logout</a>
                    <% } %> 
                    
                </div>
            </div>
        </div>
    </nav>